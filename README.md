# Наш проект Telegram-бота для проведения опросов

Группа ВБИо-404рсоб

Участники:
- Журавлев П.	: Тимлид
- Тугайбей Д.	: Разработчик

## Описание 

Телеграм бот для проведения опросов 

Поддерживает ответы текстом и ответы с выбором варианта  

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd survey-bot
```

### 2. Настройка базы данных PostgreSQL

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE survey-bot;
CREATE USER survey-bot_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE survey-bot TO survey-bot;
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните своими данными:

```bash
cp .env.example .env
```

**⚠️ ВАЖНО: Сгенерируйте секретную соль для анонимизации и собственный django-secret**

Эта соль используется для криптографического хеширования Telegram ID респондентов:

```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

Сгенерируйте ваш Django SECRET_KEY

```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```
Скопируйте сгенерированное значение и вставьте в `.env`.

Отредактируйте `.env`:

```env
# Database settings
DB_NAME=fonoquest
DB_USER=fonoquest_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Anonymization settings
# CRITICAL: Keep this secret! Never commit to Git!
ANONYMOUS_SALT=<ваша-сгенерированная-соль>

# Telegram Bot
TELEGRAM_BOT_TOKEN=<токен Telegram-бота>
```

**🔒 Важно про ANONYMOUS_SALT:**
- Это критически важный секрет! Храните в надёжном месте
- Смена соли = потеря доступа к старым данным


### 6. Выполните инициализацию БД

```bash
python manage.py makemigrations
python manage.py migrate
```

### 7. Создайте суперпользователя

```bash
python manage.py createsuperuser
```

### 8. Запуск


```bash
docker-compose up -d --build
```


## Использование

### 1. Создание опроса в админ-панели

1. Войдите в админ-панель: http://IP-приложения:8000/admin
2. Создайте новый опрос (Survey) и перейдите в визуальный редактор
3. Добавьте вопросы к опросу:
   - Укажите текст вопроса
   - Выберите тип вопроса (text/choice/voice)
   - Для вопросов типа 'choice' добавьте варианты ответов
4. Отметьте опрос как активный (is_active = True)

### 2. Работа с ботом

Респонденты могут:
- Начать работу с ботом командой `/start`
- При первом запуске заполнить анкету о себе (пол, возраст, статус, курс/стаж)
- Начать прохождение опроса через кнопку "📝 Начать опрос"
- Отвечать на вопросы согласно их типу (текст, выбор варианта, голосовое сообщение)
- Просматривать свои результаты через "📊 Мои результаты"
- Просматривать свой профиль через "👤 Мой профиль"
- Продолжить прерванный опрос
- Удалить незаконченную сессию опроса
